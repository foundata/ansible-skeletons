# Role argument validation.
#
# For information on the argument_specs.yml, see the official documentation:
# https://docs.ansible.com/ansible/latest/playbook_guide/playbooks_reuse_roles.html#role-argument-validation

---

argument_specs:

  # {{ namespace }}.{{ collection_name }}/roles/run/tasks/main.yml entry point
  main:
    short_description: "Main entry point for the {{ namespace }}.{{ collection_name }}.run role"
    options:

      run_{{ collection_name }}_state:
        type: "str"
        required: true
        choices:
          - "present"
          - "absent"

      run_{{ collection_name }}_autoupgrade:
        type: "bool"
        required: true

      run_{{ collection_name }}_service_state:
        type: "str"
        required: true
        choices:
          - "enabled"
          - "disabled"
          - "running"
          - "unmanaged"
