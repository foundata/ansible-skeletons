# Services and daemons: Manage their state, configure, or create them.

---

#- name: "Service | Create systemd service: example.service"
#  ansible.builtin.template:
#    src: "example.service.j2"
#    dest: "/etc/systemd/system/example.service"
#  notify: "restart_example"
#
#
#- name: "Service | Default | Ensure example.service state"
#  ansible.builtin.service:
#    name: "example.service"
#    state: "{{ '{{' }} 'started' if {{ role_name + '_service_state' }} in ['enabled', 'running'] else 'stopped' if {{ role_name + '_service_state' }} == 'disabled' {{ '}}' }}"
#    enabled: "{{ '{{' }} true if {{ role_name + '_service_state' }} == 'enabled' else false if {{ role_name + '_service_state' }} in ['disabled', 'running'] {{ '}}' }}"
#  when:
#    - {{ role_name + '_service_state' }} != 'unmanaged'
